{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "메인 창의 기본 권한 — 코어 기본 + M1이 쓰는 플러그인(dialog·log) + 창 조작(종료 방어의 close·destroy, 테마 동기화의 set-theme)만 허용한다. 커스텀 파일 커맨드의 경로 스코프는 capabilities가 아니라 커맨드 내부 검증이 강제한다 (단일 출처: .claude/docs/rust-commands.md#권한-capabilities)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-set-theme",
    "dialog:default",
    "log:default"
  ]
}
